"""Benchmarks behind the book's [measured] numbers.

Every script here times to Appendix B's protocol via `_harness` and records its
stack via `_env`. Run them as modules from the repository root:

    python -m bench.jax_matmul --help
"""
